# 凭据存放在 Windows Credential Manager，不写入 .env。
# 首次部署或迁移：python setup_effective_table_credentials.py --legacy-daily-report <含内嵌凭据的备份路径>
#
# Credential targets（仅名称，无 secret）：
#   AI-MAIYA/EffectiveTableCRM   → CRM 用户名 + 密码
#   AI-MAIYA/EffectiveTableExcel → Excel 工作簿密码（UserName=EffectiveTableExcel）
